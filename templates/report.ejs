<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Weekly Maintenance Report</title>
    <style>
        /* Reset CSS */
        body, html {
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
        }

        /* Container */
        .container {
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
            border: 1px solid #ccc;
        }

        /* Header */
        .header {
            text-align: center;
            margin-bottom: 20px;
        }

        /* Report Body */
        .report {
            background-color: #e0f2e9; /* Light green background */
            padding: 20px;
            border-radius: 5px;
        }

        /* Table */
        table {
            width: 100%;
            border-collapse: collapse;
        }

        th, td {
            padding: 8px;
            text-align: left;
            border-bottom: 1px solid #ddd;
        }

        th {
            background-color: #4caf50; /* Green header */
            color: white;
        }

        /* Footer */
        .footer {
            text-align: center;
            margin-top: 20px;
            color: #4caf50; /* Green footer text */
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>Weekly Maintenance Report</h1>
        </div>
        
        <div class="report">
            <table>
                <tr>
                    <th>Webmail</th>
                    <th>Image</th>
                    <th>Room</th>
                    <th>Time Available</th>
                    <th>Date Complaint Made</th>
                    <th>Is Resolved</th>
                    <th>Maintenance Issue</th>
                    <th>Maintenance Issue Category</th>
                </tr>
                <% issues.forEach(issue => { %>
                    <tr>
                        <td><%= issue.Webmail %></td>
                        <td><%= issue.ImageURL %></td>
                        <td><%= issue.Hostel %></td>
                        <td><%= issue.TimeAvailable %></td>
                        <td><%= issue.DateComplaintMade %></td>
                        <td><%= issue.IsResolved ? 'Yes' : 'No' %></td>
                        <td><%= issue.MaintenanceIssue %></td>
                        <td><%= issue.MaintenanceIssueCategory %></td>
                    </tr>
                <% }); %>
            </table>
        </div>
        
        <div class="footer">
            <p>Find attached below maintenance Issues.</p>
        </div>
    </div>
</body>
</html>
